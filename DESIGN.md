# LibTV 式画布 · Aurora Studio 设计系统

> UI Designer 交付物 —— 样式层模块化重构 + 视觉升级（暗色创意工作台）。
> **交互逻辑 / 节点连线 / 后端中转均未改动**，仅替换样式实现。

## 1. 本次重构内容
- **架构重构**：原单一 `src/styles.css` 拆分为 6 个职责单一的样式模块，由 `main.jsx` 按序导入（令牌 → 基础 → 布局 → 组件 → 面板 → 画布）。
- **视觉升级**：从浅色 "Studio Light" 升级为 **Aurora Studio 暗色创意工作台**，更贴合 AI 视频创作场景——媒体在深色背景上更突出、专业感更强。

## 2. 模块化结构
```
src/styles/
  tokens.css    设计令牌（颜色/字体/间距/圆角/阴影/动效）+ [data-theme="light"] 浅色覆盖
  base.css      重置 / 排版 / 滚动条 / 焦点环 / 动效偏好
  layout.css    应用骨架：顶栏 / 工具栏 / 状态栏 / 右侧面板 / 标签页
  components.css 节点卡片 / 把手 / 通用按钮(run/mini/del) / 输入行 / 产出缩略
  panels.css    属性面板表单控件 / 资产库网格
  canvas.css    React Flow 暗色统一（背景/连线/把手/控件/小地图/attribution）
```
令牌驱动：组件样式全部引用 `var(--*)`，换肤只改 `tokens.css`。

## 3. 色彩系统（暗色工作台，语义化）
| 角色 | Token | 值 | 用途 |
|---|---|---|---|
| 主色 | `--accent` | `#7c8cff` 亮靛蓝 | 运行按钮、输出把手、选中描边、激活标签 |
| 渐变 | `--grad-brand` | 靛蓝→紫 | logo mark、激活下划线、输出按钮 |
| 工具节点 | `--node-tool` | `#a855f7` 紫 | 工具节点边框 / 输出把手 |
| 资产节点 | `--node-asset` | `#7c8cff` 靛蓝 | 资产节点边框 / 输出把手 |
| 输入把手 | `--handle-in` | `#2dd4bf` 青 | 工具节点输入把手（颜色=可连类型提示） |
| 状态 | success/warning/error | 绿/琥珀/红 | 状态徽章与错误 |
| 中性 | `--bg #0b0e14` / `--surface #151b24` / `--border #25303f` / `--text #e6edf3` | 骨架层级 |

**把手配色规则（关键易用性）**：左侧输入=青、右侧输出=节点语义色，颜色即"可连类型"提示，与连线校验逻辑一致。

## 4. 字体 / 间距 / 圆角 / 阴影
- 字体：`Inter` → 系统 → `PingFang SC`/`Microsoft YaHei`；文件名用 `--mono`。
- 间距基准 4px；圆角 6→999(pill)；阴影在暗色下以「边框 + 微光 + 投影」表达纵深。

## 5. 组件规范（节选）
- **顶栏/状态栏**：深墨渐变 + 顶/底细描边高光，logo 方块用品牌渐变。
- **工具栏**：按钮卡片化（图标 chip + 文字），hover 上浮；脚手架工具虚线 + 降透明度。
- **节点卡片**：图标 chip 区分资产/工具；选中态靛蓝描边 + 4px 光环；运行按钮渐变 + 悬浮上浮。
- **属性面板**：表单聚焦环（靛蓝 3px）、下拉自定义箭头、删除按钮红色描边悬停填充。
- **React Flow**：点阵背景、控件/小地图浮层圆色阴影、连线靛蓝描边 + 动画虚线、把手白描边 + 阴影。

## 6. 无障碍
- 全部可交互元素 `:focus-visible` 焦点环。
- 遵循 `prefers-reduced-motion` 关闭动效。
- 语义色满足暗底对比；状态用"颜色+文字/emoji"双重表达。

## 7. 主题可切换（预留）
`tokens.css` 已含 `[data-theme="light"]` 覆盖；如需浅色，给根节点加 `data-theme="light"` 即可整体换肤（当前默认暗色）。

## 8. 应用方式（已落地）
- 删除旧 `src/styles.css`，新增 `src/styles/*.css` 六模块；`main.jsx` 改为按序导入。
- 仅调整 `Canvas.jsx` 点阵色（暗色 `#1c2330`，纯视觉）。
- 功能零改动：store / 事件 / 后端 / 节点连接逻辑保持原样。
